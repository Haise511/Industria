# Dependencies
node_modules/

# Build output
app/dist/
app/tsconfig.tsbuildinfo

# Build artifacts (excluded from repo)
build-artifacts/

# Environment
.env
.env.local
.env.*.local

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# OS
.DS_Store
Thumbs.db

# Editors / IDE
.vscode/
.idea/
*.swp
*.swo

# Vite cache
.vite/

# Local SQLite test DB (only used for ad-hoc localhost testing)
server/prisma/dev.db
server/prisma/dev.db-journal

# Local-only seed script (creates test users + awaiting_rating order on SQLite)
server/src/seed-local.ts

# Local test revert checklist (per-session, not for commit)
.LOCAL_TEST_REVERT.md
